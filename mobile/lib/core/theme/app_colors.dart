import 'package:flutter/material.dart';

/// AllôDoto — Palette de couleurs officielle (Charte Graphique V1.0)
class AppColors {
  AppColors._();

  // Couleurs Primaires
  static const Color primary = Color(0xFF1A7A4A);
  static const Color secondary = Color(0xFF2EAA6E);
  static const Color primaryDark = Color(0xFF0D5C35);

  // Couleurs Neutres
  static const Color textPrimary = Color(0xFF1E2D24);
  static const Color textSecondary = Color(0xFF5A6B62);
  static const Color backgroundLight = Color(0xFFE8F5EE);
  static const Color background = Color(0xFFF0F4F2);
  static const Color surface = Color(0xFFFFFFFF);

  // Couleurs Fonctionnelles
  static const Color success = Color(0xFF27AE60);
  static const Color warning = Color(0xFFF39C12);
  static const Color error = Color(0xFFE74C3C);
  static const Color info = Color(0xFF2980B9);
}
